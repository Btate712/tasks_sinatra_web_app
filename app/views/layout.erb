<!doctype html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="/style.css" />
    <title>Task App</title>

  </head>
  <body>

    <div class="container">
      <%= yield %>
      <% if @logged_in %>
        <form class="align-left" method="GET" action="/logout">
          <input type="submit" value="Log Out">
        </form>
      <% end %>
    </div>

  </body>
</html>
